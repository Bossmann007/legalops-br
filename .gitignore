# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
*.egg
.eggs/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
coverage.xml

# uv
.venv/
.python-version

# IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Secrets — NEVER commit
.env
.env.*
!.env.example
*.pem
*.key
secrets/

# Local agent/IDE state (Codex, Anthropic agents, etc.)
.agents/
.codex/
AGENTS.md
state/

# Release script output (generated, dated)
metrics/

# Synthetic corpus output (regenerable)
corpus/synthetic/docs/

# Audit log databases (runtime data)
*.db
audit.db
audit_*.db

# Client PII / local firm state (LGPD — NUNCA versionar dados reais do escritório)
data/
logs/
memory.local/
memory/*.local.md
clientes-aliases.json

# OS
Thumbs.db

# Slides build artifacts (npm deps not relevant to Python core)
slide/node_modules/
slide/package-lock.json
slide/yarn.lock
slide/dist/

# codebase-memory-mcp — machine-local, absolute binary path (do not ship to handoff)
.mcp.json
